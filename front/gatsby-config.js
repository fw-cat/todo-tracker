/**
 * @type {import('gatsby').GatsbyConfig}
 */

require('dotenv').config()

module.exports = {
  siteMetadata: {
    title: `Todo Tracker`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [],
}
